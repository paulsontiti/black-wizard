import React from "react";

const Mashup = () => {
  return (
    <div
      className="bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))]
    from-primary-900 to-transparent rounded-full 
    w-80 h-80 z-0 absolute transform
     blur-lg "
    ></div>
  );
};

export default Mashup;
